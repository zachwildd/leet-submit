class Submission {
  constructor(problemId, userId, userCode, testCode) {
    this.problemId = problemId;
    this.userId = userId;
    this.userCode = userCode;
    this.testCode = testCode;
  }
}

module.exports = Submission;